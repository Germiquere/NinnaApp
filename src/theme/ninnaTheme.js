import { createTheme } from "@mui/material";

export const ninnaTheme = createTheme({
  palette: {
    primary: {
      main: "#db7111",
    },
    secondary: {
      main: "#ba5e0b",
    },
    error: {
      main: "#ca2b4b",
    },
    warning: {
      main: "#ffffff",
    },
  },
});
